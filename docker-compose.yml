services:
  postgres:
    image: postgres:16
    container_name: schedule_task_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: schedule_user
      POSTGRES_PASSWORD: schedule_password
      POSTGRES_DB: schedule_task_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: schedule_task_redis
    ports:
      - "6379:6379"

volumes:
  postgres_data: